export default {
    config: {
        db: {
            type: "sqlite",
            name: "prod.db"
        },

        src: {
            fileExtension: ".js",
            folder: "/build"
        },

        subways: {
            realTimeUrl: "url",
        },

        notifications: {
            configFile: "file"
        }
    }
};